// Only public configuration belongs here. Never paste a service_role or secret key.
// Enable Anonymous Sign-Ins and run supabase/schema.sql before setting these values.
window.ECHO_COMMUNITY_CONFIG = Object.freeze({ url: '', publishableKey: '' });
