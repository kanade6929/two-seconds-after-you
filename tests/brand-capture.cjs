const{chromium}=require(process.env.ECHO_PLAYWRIGHT_MODULE||'playwright'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});for(const[w,h,mobile]of[[1440,900,false],[1280,720,false],[390,844,true],[360,800,true],[844,390,true]]){const c=await b.newContext({viewport:{width:w,height:h},isMobile:mobile,hasTouch:mobile,deviceScaleFactor:mobile?2:1}),p=await c.newPage();await p.goto('http://127.0.0.1:4173');await p.evaluate(()=>document.fonts.ready);await p.waitForTimeout(450);assert.match(await p.title(),/辰星夜/);assert.equal(await p.locator('.title-logo h1').innerText().then(s=>s.replace(/\s/g,'')),'辰星夜');const overflow=await p.evaluate(()=>[...document.querySelectorAll('button,h1,p')].filter(e=>e.getClientRects().length&&e.scrollWidth>e.clientWidth+2).map(e=>e.id));assert.deepEqual(overflow,[]);await p.screenshot({path:path.join(__dirname,`../artifacts/browser/brand-${w}-${h}.png`)});await c.close();}await b.close();console.log('辰星夜 title / font layouts passed');})().catch(e=>{console.error(e);process.exit(1);});
