// Focused visual recapture after the independent review; no puzzle state changes.
const {chromium}=require(process.env.ECHO_PLAYWRIGHT_MODULE||'playwright'),path=require('node:path');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});const c=await b.newContext({viewport:{width:390,height:844},hasTouch:true,isMobile:true,deviceScaleFactor:3});const p=await c.newPage();await p.goto('http://127.0.0.1:4173');await p.evaluate(()=>document.fonts.ready);await p.waitForTimeout(450);const dir=path.join(__dirname,'../artifacts/browser');await p.screenshot({path:path.join(dir,'mobile-final-title.png')});await p.locator('#sound').tap();await p.locator('#motion').tap();await p.waitForTimeout(200);await p.screenshot({path:path.join(dir,'mobile-final-settings.png')});await p.locator('#titleLevels').tap();await p.waitForTimeout(500);await p.screenshot({path:path.join(dir,'mobile-final-chapters.png')});await b.close();console.log('Mobile review recaptures ready');})().catch(e=>{console.error(e);process.exit(1);});
