---
name: 辰星夜
description: 夜色中的大阿卡那刻印，青色现在与暖金过去共同前行。
colors:
  night: "#090f19"
  cyan: "#b8eee8"
  gold: "#edbd88"
  text: "#d5e6e8"
  muted: "#a1b4c6"
  line: "#3c5266"
  canvas-muted: "#91a6b9"
  canvas-line: "#2b3b50"
  button: "#101c2b"
  button-hover: "#1b3441"
  primary-surface: "#213a42"
  primary-text: "#e1fffa"
  ritual-surface: "#0e1a28"
typography:
  display:
    fontFamily: '"Arcana YueSong", "Songti SC", "SimSun", serif'
    fontSize: "clamp(64px, 6.4vw, 92px)"
    fontWeight: 400
    lineHeight: 1.18
    letterSpacing: "0.12em"
  headline:
    fontFamily: '"Arcana YueSong", "Songti SC", "SimSun", serif'
    fontSize: "30px"
    fontWeight: 400
    letterSpacing: "0.08em"
  body:
    fontFamily: '"Arcana YueSong", "Songti SC", "SimSun", serif'
    fontSize: "15px"
    fontWeight: 400
    lineHeight: 1.9
  canvas-label:
    fontFamily: '"Arcana YueSong", "SimSun", serif'
    fontSize: "12px"
    fontWeight: 400
  touch-input:
    fontFamily: '"Arcana YueSong", "Songti SC", "SimSun", serif'
    fontSize: "16px"
    lineHeight: 1.7
rounded:
  square: "0px"
spacing:
  nav-gap: "8px"
  control-gap: "10px"
  touch-edge: "14px"
  chapter-gap: "18px"
components:
  button-secondary:
    backgroundColor: "{colors.button}"
    textColor: "{colors.text}"
    rounded: "{rounded.square}"
    padding: "10px 17px"
  button-secondary-hover:
    backgroundColor: "{colors.button-hover}"
    textColor: "{colors.cyan}"
  button-primary:
    backgroundColor: "{colors.primary-surface}"
    textColor: "{colors.primary-text}"
    rounded: "{rounded.square}"
    padding: "10px 17px"
  input:
    backgroundColor: "#0c1724"
    textColor: "{colors.text}"
    rounded: "{rounded.square}"
    padding: "13px"
  chapter-card:
    backgroundColor: "#101e2b"
    textColor: "{colors.text}"
    rounded: "{rounded.square}"
    padding: "18px 10px"
  ritual-panel:
    backgroundColor: "{colors.ritual-surface}"
    rounded: "{rounded.square}"
    padding: "38px 45px"
    width: "min(500px, 90vw)"
---

# Design System: 辰星夜

## Overview

**Creative North Star: "夜色中的大阿卡那刻印"**

深夜底色承托青色现在与暖金过去。纤细直线、独立牌章图腾和留白让游戏像一场安静的夜行；光点、光晕与轨迹提供柔和的时间感。成长与接纳的表达依靠行动和简短中文，不承诺占卜或治疗效果。

HTML 承担文字、按钮和表单，Canvas 承担牌阵、机关与光效。两者共用用户指定的悦宋字体和青金角色；触控布局重排内容与操作区，保留同一世界、规则和光效语言。

**Key Characteristics:**

- 夜色、青色现在、暖金过去。
- 图标与 UI 轮廓用直线，光点和轨迹可柔和弯曲。
- 全局悦宋，细线刻印，操作反馈清楚。
- PC 菜单使用系统鼠标，关内使用限速光点；手机直接触控菜单与底部单光域。
- 昔光真实重播两秒前的行动，停留与交接构成八张牌的实时合作；魔术师折印设置可撤回。

## Colors

色彩角色固定，亮度负责区分文字、机关结构和激活状态；前置令牌记录实际源值。

### Primary

- 青白 `cyan`：现在的光点、可交互强调、激活机关、悬停文字。

### Secondary

- 暖金 `gold`：两秒前的回放及其镜像、牌章编号、过去侧机关和键盘焦点。

### Neutral

- 深夜 `night`：页面底色；Canvas 叠加低对比夜空渐变。
- 浅雾 `text`：任务和主要正文；蓝灰 `muted`：HTML 说明与状态。
- 雾蓝 `canvas-muted`：Canvas 标签。结构暗线 `canvas-line` 与 HTML 边线 `line` 用于轮廓，不能代替文字色。
- 按钮与仪式面板使用各自深色表面，主按钮用更明亮的青灰底和近白文字。

**The Role Color Rule.** 青色始终表示现在，暖金始终表示过去或仪式强调；不能仅靠同一颜色的微弱明暗传达操作状态。

## Typography

所有文字使用用户指定的方正清刻本悦宋简体，原始文件为 `assets/FZfont140.TTF`，网页族名为 `Arcana YueSong`。HTML 的回退为 Songti SC、SimSun、serif；Canvas 为 SimSun、serif。用户已确认可在网站嵌入并随公开 Git 仓库发布；保留原字体，不改造字形。

标题、罗马数字、按钮、正文、表单和 Canvas 标签同属这一字体。全局关闭合成字重，标题与强调文字采用正常字重。字体预加载并使用 `font-display: swap`，加载完成后刷新场景布局缓存。

- Display：辰星夜三字字标；桌面使用前置令牌，触控使用 clamp(60px, 18vw, 82px) 与 1.15 行距，触控短横屏使用 60 px。桌面高度不超过 760 px 且宽度至少 781 px 时，字标宽 370 px、文字 72 px。
- Headline：对话面板标题；游戏标题为桌面 35 px、触控 28 px，短屏可缩小。
- Body：普通段落用舒展行距，状态、提示、卡牌说明按角色缩小；触控说明保持更易读的字号。
- Canvas label：机关基础标签使用前置令牌；触控时通过场景比例补偿，使渲染字号至少相当于 11 CSS px。节制的实际水位与预测百分比显示在杯体上方，放空标签在下方；魔术师手机端的源印、接收印标签向外排布，两枚转向机关使用紧凑的“折印”标签。
- Touch input：称呼和留言统一 16 px，避免移动浏览器输入自动放大。

**The One Font Rule.** 新增控件与画布文字继续使用 Arcana YueSong，不引入另一套标题、数字或图标字体。

## Layout

桌面标题页左侧为辰星夜字标与操作，右侧为纵向刻印牌阵。顶部导航固定，游戏标题在上方、状态与操作在下方，Canvas 填满视口；牌章页默认四列，主网格上限 1220 px。常规弹层居中，社区内容宽度上限 710 px。

触控模式由粗指针媒体条件或首次触摸启用，不只依赖窗口宽度。竖屏游戏把标题、谜题、状态、拇指控制依次分区；底部单光域高 126 px，为一整块语义化按钮，合并滑动移动与轻点确认，安全区由 `env(safe-area-inset-*)` 留白。滑动超过 8 CSS px 才开始移动并取消本次轻点。底层仍能记录原地按住 0.22 秒的手势，移动立即松开，但当前八关不需要长按；长按松手不触发确认。滑动使用青色、记录按住时使用暖金反馈。暂停、切换关卡、取消和失去触点捕获都清理手势。牌章重排为两列，标题与社区的返回按钮禁止压缩换行。

高度不超过 600 px 的触控横屏将标题与状态放到左侧 216 px 区域，谜题和控制从左边 248 px 处开始，底部控制高 98 px。通用响应断点还有宽度 1000 / 650 px 与高度 760 px，最终尺寸由 CSS 层叠共同决定。

手机相机根据可用区域、机关、预设边界与注释范围适配；关卡、窗口或底部状态可见性改变时重算，同一谜题不追着光点持续缩放。恋人本体标签沿左右方向向外排布，镜像未进入机关时也带外侧角色标签；进入机关后由机关文字承担标识，避免重复遮挡。Canvas DPR 上限为 2；这些实现事实不等同于实体手机的性能验收。

安卓移动性能更新：缓存背景、光晕和视口尺寸，只在文字改变时写入状态；轨迹缓冲按局部包围盒复用。手机曲线取样为 30 Hz 几何节点，仍逐绘制帧以曲线连接，非 30 Hz 游戏逻辑。120 Hz 回放/判定与顶层光点不降速。菜单背景目标 30 FPS、关内 60 FPS；持续约 2.2 秒帧压力才分档降低 Canvas DPR 至最低 1.25，保持逻辑尺寸和 HTML 字体，不删除粒子、呼吸或光波。

## Elevation & Depth

### 星海动效更新（2026-09-22）

以下时长覆盖本节后面的历史数值。弹层 620 ms 浮入、280 ms 淡出，按钮颜色过渡 320 ms，结尾双光汇合 2.8 秒；不延缓光点移动、两秒回放或机关逻辑。可点击框呼吸周期约 1.33 秒。

愚者在机关上方用较大悦宋字浮显基础玩法：青光代表现在、橙光重走两秒前的行动，先停留再走向门，停留多久就重播多久。手机使用屏幕坐标保持 15/12 CSS px 的两级文字，不占用机关命中区。

普通场景点击触发 1.35 秒青色双圆涟漪，游戏时钟两秒后同位置出现暖金涟漪；点击是独立事件，不从可能漏掉短按的连续按下状态推导。暂停冻结事件，重建回声/重试清除事件。胜利时仅表现层继续回放最后两秒，规则已经停止，不再给机关供能。

背景为三张 256 px 离屏星雾纹理（青蓝、灰紫、暖金）轻柔叠合，边缘透明蒙版避免矩形接缝；随缓慢位移、呼吸明暗产生流动。桌面 235 星、手机 145 星分布成宽阔斜向星河，按 CSS 像素绘制，不受关卡相机缩放影响。没有全屏实时模糊或噪声生成。

过关仪式 4.2 秒：青金大圆光波扩散，背景星点绕画面中心旋转并拖出分段渐亮弧线，末段星轨淡去、原星场浮回，再展开完成面板。手机隐藏操作区时冻结原关卡相机，防止机关瞬间缩放。减少动态保留局部渐隐、静止星空和两秒后点击回应，2.4 秒后显示完成面板，不改变规则。

声音统一为柔和正弦基音与低强度泛音，45 ms 入音、指数余韵、共享 2.8 秒卷积空间。今光点击高音、昔光低八度回应；开门采用低音到高音逐层展开的和弦，与小涟漪音效分级。限制同时振荡器数量，停止后断开节点；静音和切后台衰减总音量并停止声音，未有用户手势不启动音频。

背景音乐《星潮之间》为原创离线合成纯音乐，80 BPM / 96 秒循环，以延伸和弦、柔垫、清亮琶音、稀疏旋律和轻节拍构成。播放器 `music.js` 单次解码、单源循环，不在游戏绘制循环中调度音符；解码后软化 8 ms 编码接缝。整体受现有声音按钮控制，不新增 UI。常规增益 0.14，暂停 0.065，通关 0.055；音乐缓慢淡入，通关快速退让后再恢复。后台停止并保留播放位置，不重置关卡或影响两秒时钟。未配置额外账号/API，参考曲没有被采样或转录。

深度来自夜色的渐变层、细框、柔光与少量暗阴影。背景和机关在最下方；HTML 界面、提示、遮罩依次叠放；关卡内青色光点位于可见界面之上，菜单中隐藏并恢复系统鼠标。弹层以深色遮罩隔开场景，维持清楚的操作层次。

- 按钮悬停使用轻暗阴影 `0 3px 22px #070c1466`。
- 提示面板使用 `0 20px 55px #04081099`。
- 仪式面板使用 `0 30px 90px #02070d88`，并以外移细框形成双层轮廓。

机关资格由实时占位和稳定时间决定，视觉余光单独衰减：普通机关约 0.18 秒，愚者星门和魔术师接收印约 0.16 秒；激活接近约 0.14 秒。固定光路按每段坐标独立保存亮度，旧路径淡出、新路径淡入；星星的移动光弦和太阳实时射线直接绘制，不进入按坐标保存的余光池。太阳两枚接收面各自渐亮或渐暗。界面入场 330 ms、退出 150 ms；最后有效动作完成谜题，随后过关光波 1.1 秒。减少动态关闭装饰运动，界面切换直接完成，过关反馈缩为 0.4 秒，实时规则不变。

每张牌只呈现一个谜题。世界以四方印和顺时针接边呈现交接进度，已接好的边保留；一边完成后立即清理上一组占位与合作进度，再显示下一组角色。

可点击机关按当前动作显示 1 Hz 青金呼吸光、向外扩散的八边形细框和具体操作标签，例如“点击抵达”“转动折印”“接住微光”或“放空水杯”。能否操作由当前规则状态决定，余光不赋予操作资格；减少动态用常亮框和文字。恋人通过镜像实时合作完成，无需点击框或额外确认。

月亮的可点击状态只表示“可以确认选择”，不代表答案正确。昔光在月井稳定占位前，上方显示分枝真纹；占位后真纹完全隐藏，换成中性空菱形，并显示三个上下或左右变换的候选。三个候选的点亮、点击框与“确认倒影”文字一致，不用悬停泄露答案。窗口内一次正确点击完成，错误仅提示；昔光离井后真纹重新显现。

## Shapes

HTML 按钮、输入框和面板是直角矩形，按钮右下有细小直角刻痕；Canvas 牌框使用切角折线，机关使用八边形。不要把 HTML 矩形描述成已实现的切角裁剪。

本轮机关的可见内八边形半径为 40、方向为 π/8，点击使用同一多边形判定；普通占位进入使用内八边形，已进入后在半径 46 的外八边形释放，减少边缘细小移动造成的闪断。离开有效范围立即失去资格，视觉余光不能延续占位。恋人实时映射今光与两秒前昔光的实际位置，不自动吸附到印心。以上几何属于当前谜题实现，不规定所有后续谜题必须采用同一尺寸或机关形状。

星星为八向尖芒，月亮为凹折线，太阳为菱形核心与八向射线；每张牌拥有独立直线图腾。SVG 与 Canvas 共用 `symbols.js` 路径。光点、背景星尘、发光扩散和轨迹允许圆形与曲线。

## Components

### Title Lockup

辰星夜以原悦宋三字构成主界面字标。中央“星”上移 5 px；上方冠饰由星芒、菱形、折线与刻度组成，下方细线被中央菱形打断。装饰为内联 SVG，文字仍是可访问的 HTML h1；不把字体改造成图片。常规字标容器宽度为 min(410px, 100%)，触控上限 400 px，短横屏宽 330 px。青白文字、暖金冠饰与极轻文字光晕延续现有色彩角色。

### Buttons

细框直角、最小高度 44 px；主按钮青灰填色，次按钮深夜填色。菜单悬停和按下使用原生鼠标；只有关内控件由光点命中产生，两者使用相同视觉反馈。关内上限 420 单位/秒、1600 单位/秒²，近目标缓动防抖不变。按下有 1 px 下沉；禁用态减淡并保留语义。键盘焦点为外移 4 px 的 2 px 暖金轮廓，Tab、Enter 与 Esc 路径保留。

### Inputs / Fields

深色直角输入框，细线边界、暖金插入符，与按钮共用焦点轮廓。手机使用原生触控与输入法；桌面点击、选字、滚动、键盘和中文输入法也使用浏览器原生行为，不经光点转发。服务未配置时显示真实不可用状态并禁用提交区。

### Navigation

设置导航由图标和中文标签组成；触控或窄屏隐藏可视标签，保留可访问名称与最小触控面积。触控选中状态为暖金框与暖金图标；静音加斜线，减少动态加短横标记，均对应 `aria-pressed`。返回按钮保持完整单行。

### Cards / Containers

牌章卡以双细框组织罗马编号、直线图腾、牌名与进度文案，完整卡面可点；锁定卡保持禁用语义。仪式面板用于暂停、完成和结局，居中排版并保留外侧细框。启示面板靠近下方操作区，触控时根据可用空间限高并可滚动。

### Realtime Echo and Undo

暖金昔光严格重播两秒前的真实位置与按下状态，并保留连续轨迹。今光停留多久，昔光随后停留多久；点击不会建立永久留影。状态栏以“昔光慢两秒 · 停留会被重播”解释时间关系，机关进度细线表示当前合作累计，失去条件即清零。

下方操作区提供“撤回一步”“重建回声”“启示”“暂停”；节制把重建按钮改为“放空重来”。撤回仅用于魔术师的折印设置，同时清空旧回放重新建立回声。重建回声清空轨迹、占位与合作资格，保留魔术师设置和世界已接边；节制同时放空水杯和待兑现注水记录，不能用撤回复原旧水量。不可用操作保留禁用语义；重置谜题放在暂停面板。每关最后一步就是完成，不追加独立门印阶段。

### Puzzle Relations

八关各自用现有细线、图腾和状态文字解释实时合作：愚者连接过去守住的旅灯与现在抵达的星门；魔术师让昔光供能、光路真实穿过两枚折印；恋人让两侧镜像交错回应；节制把实填水位、虚线预测和两条暖金目标线画在同一只水杯中，上方显示实际与预测百分比；星星以同一根移动光弦同时穿过双星；月亮在昔光守井的窗口内隐藏真纹并呈现倒影选择；太阳用实际截断的射线说明遮板与两枚接收面的关系；世界以四方印、已亮边和接边进度显示顺时针交接。它们共享动作反馈，不把每关的解题几何升级成品牌规则；具体推理链见《关卡设计.md》。

### Lovers Mirror

恋人以中央镜子和两枚机关构成四光点空间：今与昔的圆形本体，加上各自带菱形圈的镜像。镜轴为 x=600，左右反照保持高度；虚线连接本体与镜像，青金角色不变。左上机关只接受两秒前昔光的镜像，右下机关只接受今光镜像，本体踏入机关不生效。镜像在机关填充和描边完成后绘制，确保点亮时仍能看到四个光点；角色标签向外放，入印镜像不再重复叠字。两个指定镜像各稳定占位 0.25 秒，再共同保持 0.5 秒即完成，不需要额外确认。

### Touch Controls

菜单直接触摸；关内底部单光域接受相对滑动与轻点确认，也可以轻触场景选目的地，但选路本身不自动确认。页面显示“滑动移动 · 轻点确认”和“无需按住 · 也可轻触场景选路”。使用与 PC 相同的光点限速和实时回放规则；底层长按记录仍保留，当前谜题没有长按任务。操作文案随输入设备切换，不能给手机继续显示“只需鼠标”。

## Do's and Don'ts

### Do:

- Do 延续青色现在与暖金过去的角色关系。
- Do 让 HTML、Canvas、罗马编号和控件共用用户提供的悦宋字体。
- Do 用直线构成图标与 UI 轮廓，允许光点、光晕和轨迹弯曲。
- Do 保留可读的 Canvas 标签、恋人外侧角色标记和清楚的键盘焦点。
- Do 将实时占位与视觉余光分离，让重建回声、撤回设置和最后一步完成立即符合规则。
- Do 区分原生菜单输入与关内光点命中，保留 PC / 手机同一游戏规则。

### Don't:

- Don't 用未点亮的暗边框色绘制说明文字。
- Don't 在同一谜题追随光点持续缩放手机场景。
- Don't 用月亮候选的悬停差异泄露答案，或让镜像被机关填充盖住。
- Don't 为手机隐藏图标状态、压断返回文案或沿用鼠标专属提示。
- Don't 将静态检查、截图或模拟环境结果表述为实体手机性能验证。
- Don't 在社区未配置时展示假留言、假点赞或成功提交状态。
