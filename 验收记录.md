# 大阿卡那重制版验收记录

## 原创背景音乐（2026-09-22）

- 86 项 Node 测试通过，0 失败、0 跳过，含五项 Canvas 像素回归；音乐改动不更改关卡逻辑和画面。
- 原创《星潮之间》：96 秒、80 BPM、32 小节，MP3 112 kbps / 32 kHz / 立体声，1,345,291 字节。FFmpeg 完整解码无错误。源码和可编辑谱面随项目保留，未用参考曲采样或旋律转录。
- Chrome、Edge、Chrome 手机触控模拟使用真实 Web Audio 解码，循环长度 96 秒；播放器消除编码边缘瞬态后，首末样本差为 0。验证首次手势前不建立播放器、开启/关闭、保留进度、重复开启不叠播、后台停止；503 音频失败不影响关卡进入。
- 自动测试验证音乐加载单例、静音时未完成解码不晚到播放、失败可重试、通关降低增益与暂停音量；现有音效节点回归保持通过。
- 没有取得参考曲音频，未进行原曲听感对照；没有实体手机或现场耳机/扬声器试听。参数、文件完整性和浏览器播放验证不等于主观音乐品质验收。

对应脚本：`tests/music.test.cjs`、`tests/music-browser.cjs`、`scripts/render-music.cjs`。发布清单与线上二进制摘要校验包含 music.js 和 MP3。

## 星海与点击回声（2026-09-22）

- 80 项 Node 测试通过，0 跳过，含五项实际 Canvas 像素测试。新增短点击独立事件、两秒回应、暂停冻结、重建清除、连续点击有界、帧率一致、星雾缓存和减少动态检查。
- Chrome、Edge、Chrome 触控模拟横竖屏全八关通过输入路径回归；弹窗变慢不修改求解窗口。原生菜单/表单、暂停继续、刷新、存储拒绝及模拟社区回归通过。
- `tests/atmosphere-browser.cjs` 以实际鼠标/触控完成点击、暂停、回应、通关、星轨回落、减少动态，捕获桌面与手机横竖屏。修复了星雾纹理边缘接缝、手机通关隐藏操作区时相机缩放；相机稳定有断言。
- `tests/audio-browser.cjs` 使用真实 Web Audio 节点验证单一音频上下文/混响、今昔音高区分与静音不再创建声音；不等同于耳机/扬声器主观试听。
- 桌面 Chrome、390×844 / DPR 3、4 倍 CPU 限速、移动光点五秒：绘制中位 3.2 ms、P95 4.2 ms、布局读取 0、重排 0。不是安卓真机 FPS，实体设备仍未验收。
- Taste/Impeccable 按既有塔罗语言做有限动效改造；截图确认提示可读、手机不遮挡机关，保留直线图腾与圆弧光效的边界。源码探测无报告，不代替视觉检查。

截图在 `artifacts/browser/*cosmos*.png`，只保留本地；无新增运行依赖或外部音频文件。

## 实时回声版（2026-09-22）

当前版本以这一节和《关卡设计.md》为准。下方静态解谜记录仅为历史证据，不代表本轮玩法。

- 73 项 Node 测试通过，含五项真实 Canvas 像素回归；八关经过 420 单位/秒、1600 加速度的公开输入求解，未写入通关或临时供能变量。不同 30/60/144 FPS 驱动下固定逻辑一致。
- 恢复真实两秒回放，不允许永久留影。反例覆盖快速扫场、重复点击、角色互换、单点冒充、过期供能开门、逐颗扫星、遮板错误方向、世界跳印、旧静态检查点注入、节制过量/不足及撤回绕过脉冲。
- Chrome / Edge 1440×900，以及 Chrome 触控模拟 390×844 / 844×390、DPR 3 全八关通过鼠标/触控输入求解。受控时钟保证测试可重复，不等同于人工试玩或真机手感。
- 暂停/继续、失焦、缩放、刷新、原生菜单/输入框、提前点击不排队、存储拒绝、中文文本输入与明确模拟社区的失败重试/XSS 回归通过。真实 Supabase 未配置。
- 新版 v4 保留 v2/v3 解锁与声音/动态设置，旧检查点不迁入，原始存档保持不变。魔术师方向可恢复，世界稳定边由 Node 路径恢复再通关，水量和回放不恢复。
- 节制过量、点击放空、再次求解，魔术师撤回与刷新设置，桌面及手机横竖屏通过；月亮在 Chrome、Edge、手机竖屏完成实时显隐、错选和正确重试。
- 手机底部单光域在横竖屏通过实际拖动事件、单击、长按不误点、手抖容差、取消、暂停及键盘确认。无关卡要求长按。
- 桌面 1280×720、1440×900、1920×1080、DPR 2，手机 360×800、390×844、430×932、844×390 检查布局和字体。另对节制 1280×720、1920×1080、360×800 检查实际画面。
- Taste/Impeccable 沿用既有视觉体系。独立复核指出手机横屏魔术师标签重叠；已将源/接收印文字外置，折印标签缩短。同一评审对该唯一 P2 修复评分 resolved，ship 限于已审范围。探测器因缺少解析依赖降级为正则，空结果不当作对比度或全站合规证明。
- Chrome 390×844 / DPR 3、4 倍 CPU 限速、连续移动光点五秒：星星绘制耗时中位3.1ms，P95 4.1ms，0次布局读取、0次布局重排。这是桌面GPU上的模拟数据，不代表安卓真机FPS。
- 本地 file:// 入口、随附字体加载和键盘开始通过。源代码语法与 git diff --check 通过。

浏览器与截图脚本：tests/browser-check.cjs、browser-regression.cjs、targeted-check.cjs、moon-check.cjs、touch-browser-check.cjs、save-migration.cjs、temperance-layout.cjs。原始截图保存在本地 artifacts/browser，不进入发布包。

## 历史：短篇推理与四光镜像版（2026-09-16）

- 68 项 Node 测试通过，包含五项真实 Canvas 像素回归。覆盖八关限速输入求解、30/60/144 FPS 固定逻辑一致、精确两秒回放与按钮边界、主动留影、撤回、存档验证、移动端提示间距和反例。
- 八关各一个谜题。实际输入路径故意加入 4-60 秒停顿，验证留影后不需要抢反应窗口。魔术师唯一光路、恋人两镜像角色、节制全部可达水量守恒与最短七次倾倒、星星不重复边、月亮不泄露正确项、太阳唯一奇偶组合、世界方向不变量均有对应测试。
- Chrome、Edge 隔离无头浏览器完整通关八关；Chrome 390×844 竖屏、844×390 横屏 / DPR 3 触控模拟也完整通关。全程使用真实页面鼠标/触控事件，不直接修改关卡或胜利状态；自动测试使用受控时钟，不是人类手感验收。
- 恋人具有中央镜子、两个机关、四个光点。本体、互换角色、不正确镜像位置均不能完成。留影修改、撤回后即时恢复镜像、暂停、刷新后重新建立留影在桌面及手机横竖屏回归通过。
- 节制倾倒与撤回、刷新水量保留、重置谜题，以及 25 秒启示出现和两级提示，在桌面与手机横竖屏通过。月亮在 Chrome、Edge、手机模拟通过隐藏真纹、错选保留、收回重看和单次正确确认。
- 手机单光域验证滑动、轻点、原地长按不误点击、抖动容差、取消、暂停恢复、键盘确认。当前谜题无需长按；长按的输入记录能力保留。
- 菜单原生鼠标、关内光点命中、提前点击不排队、反复暂停恢复、窗口调整、旧设置迁移、存储拒绝、刷新、中文文本输入与模拟社区错误恢复/XSS 回归通过。真实 Supabase 仍未配置，不将模拟服务称为社区上线。
- 浏览器布局矩阵包括桌面 1280×720、1440×900、1920×1080、1440×900 / DPR 2，手机 360×800、390×844、430×932、844×390。字体加载、HTML 宽度溢出、谜题目标不被 UI 遮挡均检查。24 张实际关内截图覆盖八关与三类布局。
- 保留轨迹抗闪动、粒子、光晕与 1.1 秒过关光波；光路和太阳光窗改为独立渐亮/渐熄，逻辑不受视觉余光影响。手机镜像标签改放外侧，月亮真纹与说明拉开距离。
- 性能抽样：桌面 Chrome 手机视口 390×844、DPR 3、4 倍 CPU 限速、移动约 5 秒。绘制中位数 2.8 ms、P95 3.8 ms，主线程任务 0.792 秒，布局测量 2 次。rAF 中位间隔 32.6 ms；此数据不是安卓真机 FPS，也不是相同谜题的版本对比。
- 视觉检测器缺少 HTML 解析依赖，降级为正则扫描；其字体/颜色记录提示不构成完整对比度验收。保留实际截图审查与测试证据。
- 独立视觉复核提出的两项修复均已确认 resolved：月亮三个候选悬停时仅显示一条「确认倒影」提示；节制点击提示与水量数字明确分开。复核覆盖桌面、手机竖屏和横屏，本次 verdict 为 ship，仅代表这两项修复确认。

当前截图与测量输出在本地 `artifacts/browser/`，不提交到网站。未测试实体安卓/iOS GPU、实际中文输入法候选窗、真实共享社区与新玩家首通时长。

## 手机单光域操作（2026-09-16）

- 移动、确认和持续按住合并到同一底部按钮，取消独立确认键。手势区分轻点、滑动和原地按住；滑动抬手、长按松手、触摸取消均不额外确认。
- 65 项 Node 测试通过，包含 5 项真实 Canvas 像素回归及 5 项新增手势测试：抖动容差、重复触发、第二触点、取消、30/60/144 FPS 按住起点与回放一致。
- Chrome 手机 390×844 竖屏、844×390 横屏模拟均完整通过八关，使用场景选路及合并光域确认/按住。单独的 `tests/touch-browser-check.cjs` 使用光域滑动到魔术师折印并验证轻点仅旋转一次、长按不旋转、抖动不移位、拖动松开按住、触摸取消、暂停恢复及键盘确认。
- 手机光域的闲置、滑动、长按状态已截图检查；360/390/430 px 竖屏和 844 px 横屏的字体与布局检查通过。手势反馈只在状态变化时写入 DOM，不增加粒子或改变 Canvas 渲染预算。
- Chrome 桌面原生菜单、光点命中、暂停、缩放、刷新、存储拒绝、旧设置及模拟留言安全回归通过。没有实体安卓手机，不将触控模拟称为真机手感或帧率验收。

## 月亮单选记忆调整（2026-09-16）

- 60 项 Node 测试通过：增加未启动显示单目标、启动隐藏目标、同一点击反馈不泄露正误、失守恢复目标及单次选择推进的回归。
- `tests/moon-check.cjs` 在 Chrome、Edge 和 Chrome 手机触控模拟中通过三轮完整输入求解、错选不推进、离井恢复目标；截图检查未启动/启动两种状态。未进行实体安卓机人工验收。
- 正确与错误选项均可显示「确认选择」，点击后才判对错；目标不再以正确项独有的出口样式提前暴露。三轮阶段存档保留，取消每轮连续点两枚。

## 安卓 Chrome 移动卡顿优化（2026-09-16）

- 58 项 Node 测试通过，含新增手机轨迹像素测试，共 5 项像素回归；自适应像素密度不改变逻辑坐标，背景/光晕缓存和局部轨迹缓冲有专门测试。
- 性能对比脚本 `tests/performance-check.cjs`：390×844 / DPR 3、安装版 Chrome 无头、4 倍 CPU 限速，比较 9ebedf9 与优化版本，持续移动约 5 秒。
- 本次记录：主线程任务 1.157 → 0.895 秒（约 -23%），绘制调用中位数 3.6 → 3.1 ms、P95 4.7 → 3.9 ms，渐变创建 11806 → 7179（约 -39%），布局 170 → 2（仅自适应重分配）。这些是受控桌面浏览器数据，不是安卓真机 FPS。
- 环境 rAF 中位间隔仍约 31.2 ms；没有依据宣称真机达到 60 FPS。实际安卓设备改善幅度仍需用户刷新测试。
- 手机横竖屏八关输入回归、桌面菜单与暂停/存档回归、渐隐和旋转检查继续通过。保留粒子、光晕、开门光波与准确两秒回放。

## 本次合作与关卡整合版（2026-09-16）

- 55 项 Node 测试全部通过，含 4 项 Canvas 像素回归。八关真实限速输入求解、各阶段恢复、空间占位容差、余光失效、配对按住、双星同弦、两次真纹顺序、实际遮挡、天平力矩、世界双片联转及连点反例均已覆盖。
- Chrome、Edge 隔离无头浏览器通过新版八关真实鼠标输入求解；Chrome 390×844 竖屏、844×390 横屏触控模拟也完成八关。包含重做的节制天平、世界拼环，各关均在本身机关上结束，没有独立的通用门印阶段。
- 可点击提醒：仅真实允许点击时出现，断电余光与暂停不显示；常规模式呼吸闪光，减少动态使用常亮边框与文字。
- 换阵：旧阵 0.18 秒渐隐、新阵 0.26 秒渐亮；过渡期间冻结时钟、回放、光点和点击。单元测试及桌面/手机浏览器截图检查了渐隐、渐入、世界旋转中间态。
- 世界存档：真实输入转动后暂停、刷新、点击继续，保留环片方向，不恢复临时供能。测试用的已解锁存档来自完整八关求解，未直接修改运行中的进度。
- 新天平六个落点、世界环片与可点击提醒已检查手机排版；世界底部与环片相交的通用进度横线已移除。
- 桌面 1280×720、1440×900、1920×1080、1440×900 / DPR 2，手机 360×800、390×844、430×932、844×390 / DPR 2 或 3 重新截图、检查字体加载及宽度溢出。
- Chrome 菜单、原生输入/滚动、光点命中、提前点击不排队、暂停继续、缩放失焦、旧设置迁移、存储拒绝及刷新续玩回归通过。
- 自动浏览器使用受控时钟。这证明可解性与输入路径，不等同于初次玩家手感、理解成本或实体手机帧率验收。

## 此前版本记录（以下为历史，不是当前关卡规则）

- 49 项 Node 测试：规则、限速、防抖、准确两秒插值、按下/松开采样边界、30/60/144 FPS 固定步进一致性、阶段清理、存档验证、八关真实限速路线、快速扫场/单点/余光/提前点击/穿墙/抄近路等反例。
- 其中 4 项使用真实 Canvas 栅格像素，覆盖交叉闪动、折返、停留渐隐、接缝亮珠、离屏残留和 DPR 2；不是浏览器帧率测试。
- 安装版 Chrome 与 Edge 的**隔离无头浏览器**，真实鼠标事件经过完整网页控制器，八关谜题与最终交接全部完成。没有直接设置阶段或通关变量。用受控时钟加速运行，不等同于人工手感评价。
- 两种浏览器在 1280×720、1440×900、1920×1080 和 1440×900 / DPR 2 生成截图；字体加载完成后检查宽度溢出。
- 全局字体已换为用户提供并授权发布的方正清刻本悦宋简体，浏览器检查实际字体加载，正文、按钮、数字与 Canvas 使用一致字体，源文件及副本 SHA-256 相同。
- Chrome 手机触控模拟：390×844 竖屏、844×390 横屏均通过八关完整触控输入求解及最后交接；另检验滑动区拖动。360×800、390×844、430×932、844×390 在 DPR 2/3 下截图和布局溢出检查。
- Chrome 补充回归：主页/选关/暂停/完成/留言恢复系统鼠标，关内仍按光点命中；菜单立即点击，关内提前点击不排队，切换不重复触发；反复暂停/继续、冻结回放、窗口调整、失焦、Esc、刷新续玩、旧设置迁移、存储拒绝、键盘开始后首个鼠标事件不瞬移。
- 社区客户端使用**明确模拟的传输**验证匿名身份单次请求、分页、点赞/取消、文字长度、失败后重试。浏览器模拟服务验证原生鼠标聚焦、中文输入、键盘选字、滚动、失败保留正文、HTML 作为纯文本显示。
- 独立视觉审查发现星星关标签被下一行机关遮挡、未点亮标签对比度不足；分别改为外侧标签与独立高对比度文字色。
- 手机审查补齐声音/减少动态的可见选中状态、触控首页文案及选关返回按钮禁止挤压换行。
- 游戏最终定名「辰星夜」，首页字标、网页标题与文档同步；新字标在 1440×900、1280×720、390×844、360×800、844×390 检查，悦宋三字与星冠均完整显示，无文字宽度溢出。
- Chrome 无头浏览器直接通过 file:// 打开本地入口、加载随附字体并用键盘开始第一关，无脚本错误。
- 2026-09-16 输入调整：光点速度 360 → 420（约 +17%），加速度 1400 → 1600；保留两秒延迟和所有占位门槛。Chrome、Edge、手机竖屏及横屏重新通过八关与最终交接。新增全部八扇门的提前连点、断电余光及合法交接回归。
- 检查发现并修复时间累计浮点误差可能使回放按下/松开晚一逻辑帧的问题。只在 0.1 微秒以内对齐时间样本，不改变实际延迟或有效供能时长。
- 受控浏览器测试固定时钟，避免自动测试执行耗时挤占合作窗口；短横屏菜单按钮先滚入可见区再点击。以上为测试稳定性处理，不放宽游戏判定。

## 尚未验收 / 不作承诺

- 没有真实 Supabase 项目，不能完成两个独立访客会话的公共数据同步、真实 RLS 拒绝、后台删除和服务限额验证；接入后按 `supabase/接入说明.md` 操作。
- 中文文字输入已测，但操作系统中文输入法候选窗、长文鼠标拖选与滚动列表的全面人工验收未完成。
- 桌面 GPU 实际 30/60/144 Hz 流畅度、现场音量与音色、首次玩家 25—40 分钟时长尚未实测。
- 手机现为新增目标，已完成触控和横竖屏模拟验证；没有实体 iPhone、安卓设备，Safari/iOS 真机、移动端软键盘安全区与低端机帧率仍未实测。

浏览器脚本：`tests/browser-check.cjs`、`tests/browser-regression.cjs`。新增 `tests/targeted-check.cjs` 检查换阵、旋转和世界刷新存档，需先运行完整浏览器生成已解锁存档。截图在本地 `artifacts/browser`，未提交到游戏运行资源。GitHub CI 默认不安装 Canvas 包，因此明确跳过 5 项像素测试，其余必须通过后才能发布。
