# 大阿卡那重制版验收记录

## 安卓 Chrome 移动卡顿优化（2026-09-16）

- 58 项 Node 测试通过，含新增手机轨迹像素测试，共 5 项像素回归；自适应像素密度不改变逻辑坐标，背景/光晕缓存和局部轨迹缓冲有专门测试。
- 性能对比脚本 `tests/performance-check.cjs`：390×844 / DPR 3、安装版 Chrome 无头、4 倍 CPU 限速，比较 9ebedf9 与优化版本，持续移动约 5 秒。
- 本次记录：主线程任务 1.157 → 0.895 秒（约 -23%），绘制调用中位数 3.6 → 3.1 ms、P95 4.7 → 3.9 ms，渐变创建 11806 → 7179（约 -39%），布局 170 → 2（仅自适应重分配）。这些是受控桌面浏览器数据，不是安卓真机 FPS。
- 环境 rAF 中位间隔仍约 31.2 ms；没有依据宣称真机达到 60 FPS。实际安卓设备改善幅度仍需用户刷新测试。
- 手机横竖屏八关输入回归、桌面菜单与暂停/存档回归、渐隐和旋转检查继续通过。保留粒子、光晕、开门光波与准确两秒回放。

## 本次合作与关卡整合版（2026-09-16）

- 55 项 Node 测试全部通过，含 4 项 Canvas 像素回归。八关真实限速输入求解、各阶段恢复、空间占位容差、余光失效、配对按住、双星同弦、两次真纹顺序、实际遮挡、天平力矩、世界双片联转及连点反例均已覆盖。
- Chrome、Edge 隔离无头浏览器通过新版八关真实鼠标输入求解；Chrome 390×844 竖屏、844×390 横屏触控模拟也完成八关。包含重做的节制天平、世界拼环，各关均在本身机关上结束，没有独立的通用门印阶段。
- 可点击提醒：仅真实允许点击时出现，断电余光与暂停不显示；常规模式呼吸闪光，减少动态使用常亮边框与文字。
- 换阵：旧阵 0.18 秒渐隐、新阵 0.26 秒渐亮；过渡期间冻结时钟、回放、光点和点击。单元测试及桌面/手机浏览器截图检查了渐隐、渐入、世界旋转中间态。
- 世界存档：真实输入转动后暂停、刷新、点击继续，保留环片方向，不恢复临时供能。测试用的已解锁存档来自完整八关求解，未直接修改运行中的进度。
- 新天平六个落点、世界环片与可点击提醒已检查手机排版；世界底部与环片相交的通用进度横线已移除。
- 桌面 1280×720、1440×900、1920×1080、1440×900 / DPR 2，手机 360×800、390×844、430×932、844×390 / DPR 2 或 3 重新截图、检查字体加载及宽度溢出。
- Chrome 菜单、原生输入/滚动、光点命中、提前点击不排队、暂停继续、缩放失焦、旧设置迁移、存储拒绝及刷新续玩回归通过。
- 自动浏览器使用受控时钟。这证明可解性与输入路径，不等同于初次玩家手感、理解成本或实体手机帧率验收。

## 此前版本记录（以下为历史，不是当前关卡规则）

- 49 项 Node 测试：规则、限速、防抖、准确两秒插值、按下/松开采样边界、30/60/144 FPS 固定步进一致性、阶段清理、存档验证、八关真实限速路线、快速扫场/单点/余光/提前点击/穿墙/抄近路等反例。
- 其中 4 项使用真实 Canvas 栅格像素，覆盖交叉闪动、折返、停留渐隐、接缝亮珠、离屏残留和 DPR 2；不是浏览器帧率测试。
- 安装版 Chrome 与 Edge 的**隔离无头浏览器**，真实鼠标事件经过完整网页控制器，八关谜题与最终交接全部完成。没有直接设置阶段或通关变量。用受控时钟加速运行，不等同于人工手感评价。
- 两种浏览器在 1280×720、1440×900、1920×1080 和 1440×900 / DPR 2 生成截图；字体加载完成后检查宽度溢出。
- 全局字体已换为用户提供并授权发布的方正清刻本悦宋简体，浏览器检查实际字体加载，正文、按钮、数字与 Canvas 使用一致字体，源文件及副本 SHA-256 相同。
- Chrome 手机触控模拟：390×844 竖屏、844×390 横屏均通过八关完整触控输入求解及最后交接；另检验滑动区拖动。360×800、390×844、430×932、844×390 在 DPR 2/3 下截图和布局溢出检查。
- Chrome 补充回归：主页/选关/暂停/完成/留言恢复系统鼠标，关内仍按光点命中；菜单立即点击，关内提前点击不排队，切换不重复触发；反复暂停/继续、冻结回放、窗口调整、失焦、Esc、刷新续玩、旧设置迁移、存储拒绝、键盘开始后首个鼠标事件不瞬移。
- 社区客户端使用**明确模拟的传输**验证匿名身份单次请求、分页、点赞/取消、文字长度、失败后重试。浏览器模拟服务验证原生鼠标聚焦、中文输入、键盘选字、滚动、失败保留正文、HTML 作为纯文本显示。
- 独立视觉审查发现星星关标签被下一行机关遮挡、未点亮标签对比度不足；分别改为外侧标签与独立高对比度文字色。
- 手机审查补齐声音/减少动态的可见选中状态、触控首页文案及选关返回按钮禁止挤压换行。
- 游戏最终定名「辰星夜」，首页字标、网页标题与文档同步；新字标在 1440×900、1280×720、390×844、360×800、844×390 检查，悦宋三字与星冠均完整显示，无文字宽度溢出。
- Chrome 无头浏览器直接通过 file:// 打开本地入口、加载随附字体并用键盘开始第一关，无脚本错误。
- 2026-09-16 输入调整：光点速度 360 → 420（约 +17%），加速度 1400 → 1600；保留两秒延迟和所有占位门槛。Chrome、Edge、手机竖屏及横屏重新通过八关与最终交接。新增全部八扇门的提前连点、断电余光及合法交接回归。
- 检查发现并修复时间累计浮点误差可能使回放按下/松开晚一逻辑帧的问题。只在 0.1 微秒以内对齐时间样本，不改变实际延迟或有效供能时长。
- 受控浏览器测试固定时钟，避免自动测试执行耗时挤占合作窗口；短横屏菜单按钮先滚入可见区再点击。以上为测试稳定性处理，不放宽游戏判定。

## 尚未验收 / 不作承诺

- 没有真实 Supabase 项目，不能完成两个独立访客会话的公共数据同步、真实 RLS 拒绝、后台删除和服务限额验证；接入后按 `supabase/接入说明.md` 操作。
- 中文文字输入已测，但操作系统中文输入法候选窗、长文鼠标拖选与滚动列表的全面人工验收未完成。
- 桌面 GPU 实际 30/60/144 Hz 流畅度、现场音量与音色、首次玩家 25—40 分钟时长尚未实测。
- 手机现为新增目标，已完成触控和横竖屏模拟验证；没有实体 iPhone、安卓设备，Safari/iOS 真机、移动端软键盘安全区与低端机帧率仍未实测。

浏览器脚本：`tests/browser-check.cjs`、`tests/browser-regression.cjs`。新增 `tests/targeted-check.cjs` 检查换阵、旋转和世界刷新存档，需先运行完整浏览器生成已解锁存档。截图在本地 `artifacts/browser`，未提交到游戏运行资源。GitHub CI 默认不安装 Canvas 包，因此明确跳过 5 项像素测试，其余必须通过后才能发布。
